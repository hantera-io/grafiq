# radio

Interactive — click to flip. Use `selected` (or `checked`) to start enabled.
See also [checkbox](/components/checkbox) and [toggle](/components/toggle).

```grafiq
col gap=10
  radio "Standard shipping" selected
  radio "Express shipping"
```
