# toggle

An on/off switch (alias: `switch`). Interactive — click to flip. Use `on` (or
`checked`) to start enabled. See also [checkbox](/components/checkbox) and
[radio](/components/radio).

```grafiq
col gap=10
  toggle "Beta features" on
  toggle "Email notifications"
```
