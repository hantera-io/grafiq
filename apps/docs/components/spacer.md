# spacer

An invisible flexible gap that pushes siblings apart inside a
[row](/components/row) or [col](/components/col).

```grafiq
row gap=8 w=320
  button "Cancel"
  spacer
  button "OK" primary
```
