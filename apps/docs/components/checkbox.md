# checkbox

Interactive — click to flip. Use `checked` to start enabled. See also
[radio](/components/radio) and [toggle](/components/toggle).

```grafiq
col gap=10
  checkbox "Remember me" checked
  checkbox "Subscribe to newsletter"
```
