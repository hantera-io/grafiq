# Use the classic node_modules layout instead of Yarn PnP.
# Vite/VitePress and the TypeScript source aliases in this repo expect a real
# node_modules tree, so PnP is intentionally disabled.
nodeLinker: node-modules
